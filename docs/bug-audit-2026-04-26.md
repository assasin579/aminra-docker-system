# Bug Audit 2026-04-26 — 4 Priority Workflows

**Scope:** Tạo doc từ template · AI eval · Submission cycle · Cert issuance.
**Method:** Two parallel deep-trace agents (Opus). 80 bugs found.

---

## Severity breakdown

| Severity | Count | Action |
|---|---|---|
| 🔴 **Critical** (security / data loss / cross-tenant) | 13 | Fix BEFORE next CB demo |
| 🟠 **Major** (state machine / correctness) | 28 | Fix in 1-week sprint |
| 🟡 **Minor / UX** | 39 | Backlog, batch later |
| **Total** | **80** | — |

---

## 🔴 Top 13 Critical (must fix immediately)

| # | Bug | File | Why critical |
|---|---|---|---|
| C1 | `replace-document` works on **approved** submissions → cert can cover doc CB never reviewed | `submission_router.py:1073` | Compliance violation, falsifies cert |
| C2 | `approve-final` overwrites AI compliance_score with NULL when CB doesn't fill eval | `submission_router.py:1043` | Data loss — AI scoring history wiped silently |
| C3 | `finalize` does `DELETE submissions` → CASCADE wipes `submission_revision_requests` history | `submission_router.py:1206` + migration 010 | Audit trail destroyed (regulatory risk) |
| C4 | `cert_decision` revoke updates ALL certs of business across providers | `audit_router.py:1105` | Cross-provider write leak |
| C5 | `viewDoc` puts JWT in URL querystring → leaks via Referer/logs/history | `submissions/page.tsx:346` + `certificate_router.py:439` | Security: token can be exfiltrated |
| C6 | `documents/{id}/preview` & `/file` allow provider whole-tenant access via OR clause | `document_router.py:359-372, 460-471` | Cross-tenant doc leakage |
| C7 | `/ingest` no path-traversal sanitization on filename → cross-tenant file overwrite | `app.py:1133` | File system tampering |
| C8 | `previous_context` from FE injected raw into LLM prompt → prompt injection | `upload/page.tsx:361` + `evaluate.py:196` | LLM controllable by attacker (score=100, false reports) |
| C9 | `get_revision_history` `or (not user.get('is_owner'))` allows ANY auditor to read ANY CB's revisions | `submission_router.py:807-816` | Cross-tenant info leak |
| C10 | Cert number generation race → duplicate `HALAL-2026-NNNN` possible | `certificate_router.py:88` | Public verify breaks; integrity gone |
| C11 | Cert PDF written to disk BEFORE INSERT → orphan PDFs if INSERT fails | `certificate_router.py:125-136` | Disk fills + verify 404 |
| C12 | `/evaluate` & `/ingest` anonymous → unauth LLM cost burner | `app.py:1133, 1164` | DoS / cost abuse |
| C13 | `/rewrite` no auth + no rate limit | `app.py:1282` | Same as C12 |

---

## 🟠 Top 28 Major

**Workflow 1 — Templates (4):**
- W1-M1: lang fallback reads root dir if `vi/` missing → wrong-language ref content fed to LLM (same class as halal_policy bug we just fixed)
- W1-M2: `compliance_score` column vs `evaluation_result` JSON not synced (two sources of truth)
- W1-M3: BG eval failure resets to `uploaded` status — no `eval_failed` state → indistinguishable from never-attempted
- W1-M4: `parse_json_safe` fallback returns score=50 silently on LLM failure → user thinks doc is borderline-passing

**Workflow 2 — AI eval (5):**
- W2-M1: Double-click on "Đánh giá" spawns 2 parallel BG tasks (no row lock CAS)
- W2-M2: BG eval orphaned on container restart (no startup recovery sweep)
- W2-M3: `aminra_eval_<userid>` falls back to `'anon'` → cross-user leak on shared device
- W2-M4: `extracted_text[:12000]` persisted in localStorage indefinitely (privacy)
- W2-M5: `validate_upload` BE 50MB vs FE message 20MB; zip-bomb defense missing

**Workflow 3 — Submission cycle (16):**
- W3-M1: `submissions.status` no DB CHECK constraint → arbitrary values via raw SQL
- W3-M2: 49 status transition matrix unenforced — provider can stomp any status
- W3-M3: `save_evaluation` auto-promotes assigned→reviewing on EMPTY checklist
- W3-M4: `request_revision` doesn't validate `document_id` belongs to submission
- W3-M5: `replace_submission_document` appends new doc when `old_doc_id` not in array (silent bloat)
- W3-M6: `replace_submission_document` doesn't revert status `revision_required → reviewing`
- W3-M7: `approve-final` doesn't require any saved evaluation → CB can approve without scoring
- W3-M8: `approve-final` clobbers entire `evaluation_result` JSON (loses AI clauses/red_flags)
- W3-M9: `approve-final` mutates `uploaded_at=NOW()` → breaks revision history ordering
- W3-M10: `approve-final` notification race (2 calls = 2 noti)
- W3-M11: `business_score` doc_score uses ALL business docs not filtered by provider (gameable)
- W3-M12: `business_score` audit_score includes incomplete visits
- W3-M13: `assign_auditor` resets approved → assigned (permanent DOS until manual fix)
- W3-M14: Stale eval rows orphaned when auditor reassigned
- W3-M15: `finalize` lacks `is_owner=true` check (non-owner business member can finalize)
- W3-M16: `request-renewal` reuses original `document_ids` even if deleted

**Workflow 4 — Cert (3):**
- W4-M1: Cert decision via `/audits/.../decision` bypasses `revoke_cert` lifecycle (no reason, no audit log)
- W4-M2: `audit_logs` REVOKE PUBLIC ineffective if app role owns table
- W4-M3: Cert expiry day-of-month dropped to 28 (Jan 31 → Jan 28 instead of 31)

---

## 🟡 Minor / UX (39 items, summary)

- 11 callsites still use `err.detail || ...` raw → "[object Object]" risk → bulk migrate to `parseApiError`
- SW typo `/api/api/audits/` → audit cache rules dead
- `auto:date` formatting hardcoded `vi-VN` regardless of `lang`
- Disabled docxtemplater regex misses `[a-z0-9_]+` (digits)
- Reset error on Back button missing
- `analyzeProgress` interval not cleared on unmount
- Status indicator + step progress UX issues
- Various notification deep-links to `/submissions` instead of `?id=...`
- `received_submissions` n+1 query (perf)
- `set_deadline` accepts past dates
- ... (full list in agent reports)

---

## 📋 Comprehensive Test Plan

### Layer 1 — Backend integration (pytest + respx) — ~50 tests

**State machine tests (priority):**
```
test_submission_status_check_constraint_blocks_garbage
test_update_status_matrix_only_legal_transitions  # 49-cell matrix
test_save_eval_empty_does_not_promote_to_reviewing
test_approve_final_requires_evaluation
test_approve_final_preserves_ai_compliance_score
test_approve_final_merges_evaluation_result_not_clobbers
test_approve_final_idempotent_under_concurrent_calls
test_assign_auditor_rejects_on_approved
test_finalize_requires_owner_role
test_finalize_does_not_cascade_delete_revision_history
```

**Cross-tenant leak tests (priority):**
```
test_documents_preview_blocks_cross_tenant_provider_access
test_revision_history_blocks_cross_tenant_auditor
test_cert_decision_only_affects_caller_provider_certs
test_replace_document_rejects_foreign_doc_id
test_request_revision_rejects_foreign_document_id
```

**Race condition tests:**
```
test_eval_endpoint_atomic_under_concurrent_calls  # CAS pattern
test_issue_cert_concurrent_no_duplicate_number
test_approve_final_no_double_notification
```

**Cert lifecycle:**
```
test_issue_cert_pdf_failure_rolls_back
test_cert_decision_revoke_writes_full_metadata
test_cert_expiry_date_preserves_day_of_month_31
test_replace_document_blocks_on_approved_status
```

**File integrity:**
```
test_ingest_rejects_path_traversal_filename
test_ingest_requires_auth
test_evaluate_anonymous_returns_401
test_template_lang_fallback_no_root_pollution  # ours from W1-M1
test_validate_upload_rejects_zip_bomb
```

**LLM safety:**
```
test_previous_context_sanitized_no_prompt_injection
test_eval_failure_returns_is_fallback_flag_not_score_50
test_failed_eval_status_eval_failed_not_uploaded
```

**Scoring math:**
```
test_business_score_filters_to_provider_approved_docs_only
test_business_score_audit_only_completed_visits
test_compliance_score_column_jsonb_invariant
```

### Layer 2 — Frontend unit (Vitest + RTL) — ~25 tests
- All 11 `err.detail` callsites → `parseApiError` migration verified
- `validatePassword` boundary (≤9, no upper/lower/digit, exact 10)
- `auto:date` lang-aware
- docxtemplater regex with `address_2`
- Step indicator state on Back navigation
- Logout clears all `aminra_*` storage keys
- Upload page restore uses user-id (never 'anon')

### Layer 3 — E2E (Playwright) — ~15 scenarios
- Workflow 1 happy path: register → fill profile → generate → 2 docx files
- Workflow 2: upload → AI eval → see report → re-upload v2 → previous_context correctly derived
- Workflow 3: submit → CB review → request revision with per-doc severity → DN resubmit → CB approve → DN finalize
- Workflow 4: cert issued → public verify → revoke with reason → public verify shows revoked
- Cross-tenant: provider P cannot view tenant Q's docs even via direct URL
- Concurrent: 2 admins issue cert at same time → no duplicate number
- Token in URL detection: scan all download/preview links for `?token=`

### Layer 4 — Static guards (existing pattern) — ~10
- No `err.detail` raw uses (extend existing spec 26)
- All endpoints serving mutable content set `Cache-Control: no-store`
- All admin endpoints use `_require_admin` not raw role check
- Token never in URL querystring (regex scan)

**Coverage target:** 80%+ on submission_router, certificate_router, audit_router, evaluate.py.

---

## 🎯 Recommended Fix Order

### Phase 1 — Critical (~3 ngày)
Fix 13 critical bugs. Block any production beta until done. Add regression test for each.

### Phase 2 — Major batch A: state machine integrity (~2 ngày)
- DB CHECK constraint on `submissions.status`
- Matrix-based transition enforcement in `update_submission_status`
- `approve-final` requires eval + COALESCE preserve scores
- Block illegal `assign_auditor` on approved
- Add `eval_failed` doc status

### Phase 3 — Major batch B: scoring + composite (~1 ngày)
- `business_score` filter by provider
- audit_score completed-only filter
- Single source of truth for compliance_score (drop column or sync)

### Phase 4 — Minor / UX cleanup (~1 ngày)
- Bulk `parseApiError` migration
- SW path typo fix
- Date format lang-aware
- Notification deep-links
- Other UX polish

**Total estimated effort:** 7 dev-days for full coverage.

---

## ⚠️ Decisions blocking fixes (need user input)

1. **W3-M16: `request-renewal`** — strip missing docs vs reject? Product call.
2. **W3-#29: business members policy** — non-owner members reading sibling submission revisions: allow or deny?
3. **W3-M9: `approve-final` mutates `uploaded_at`** — keep behavior or split into `cb_approved_at` column?
4. **W4-M2: audit_logs immutability** — change app DB role permissions, or just add TRIGGER?
5. **W3-#34: SLA from original or per-round** — product policy.

These cannot be auto-decided. Request your input before fixing.
