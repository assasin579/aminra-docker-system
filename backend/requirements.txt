# aminra Vietnam — Dependencies

# Document processing
pymupdf>=1.23.0
python-pptx>=1.0.0
python-docx>=1.1.0
markitdown[pptx,pdf]>=0.0.1

# Vector DB
qdrant-client>=1.7.0

# Embeddings
sentence-transformers>=2.6.0
torch>=2.0.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
gunicorn>=21.0.0
python-multipart>=0.0.9

# File upload validation
python-magic>=0.4.27

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
requests>=2.31.0

# Auth
asyncpg>=0.29.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0
python-jose[cryptography]>=3.3.0
pydantic[email]>=2.0.0

# Database migrations
alembic>=1.13.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Email service
aiosmtplib>=3.0.0            # async SMTP client
jinja2>=3.1.0                # email template rendering

# Async job queue (Redis-backed)
arq>=0.26.0
redis>=5.0.0

# Blockchain anchor (Polygon + smart contract)
web3>=7.0.0                  # Ethereum/Polygon RPC client
py-solc-x>=2.0.0             # Solidity compiler binding
eth-account>=0.13.0          # wallet + signing
opentimestamps-client>=0.7.0 # Bitcoin mega-anchor via calendar servers (free)

# Error tracking + observability
sentry-sdk[fastapi]>=2.0.0
prometheus-client>=0.20.0    # /metrics exposition
starlette-prometheus>=0.10.0 # ASGI middleware: HTTP histogram + counter per route
structlog>=24.1.0            # structured JSON logging with bound context
qrcode[pil]
reportlab

# ── Testing ──────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0       # async test support for FastAPI routes
pytest-cov>=5.0.0            # coverage tracking
httpx>=0.27.0                # HTTP client for integration tests
respx>=0.21.0                # mock OpenRouter/DeepSeek in RAG tests
pytest-mock>=3.12.0          # convenient mock fixture
aiosmtpd>=1.4.0              # fake SMTP server for mailer integration tests
fakeredis>=2.20.0            # in-process Redis stub for unit tests
eth-tester[py-evm]>=0.13.0b1 # in-memory EVM for smart contract tests

# ── Web Push (PWA notifications) ─────────────────────────────────────
pywebpush>=2.0.0             # VAPID-signed delivery to browser push services
python-dateutil>=2.9.0       # relativedelta for cert expiry day-of-month preserving
